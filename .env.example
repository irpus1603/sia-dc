# SIA-DC Broker Configuration Example
# Copy this file to .env and adjust values as needed

# Logging
LOG_LEVEL=INFO

# SIA-DC TCP Server Settings
SIA_HOST=                      # Empty binds to all interfaces (0.0.0.0)
SIA_PORT=65100                 # Default SIA-DC port

# Allowed Accounts (comma-separated)
SIA_ACCOUNTS=AAA,BBB,CCC

# Optional: AES Encryption Keys (comma-separated, must be 16/24/32 chars)
# Leave empty for unencrypted communication
SIA_KEYS=,1234567890123456,   # Empty = no encryption, 16 chars = AES-128

# Forwarding Target API
FORWARD_URL=http://localhost:9000/ingest
FORWARD_AUTH_HEADER=          # e.g., Bearer your-token-here
FORWARD_COOKIE=               # e.g., sid=abc123; full_name=John
FORWARD_TIMEOUT=5.0
FORWARD_MAX_RETRIES=5
FORWARD_RETRY_BASE_DELAY=0.5

# Extra Headers (semicolon-separated key:value pairs)
# Example: Content-Type:application/json;X-Custom:value
FORWARD_EXTRA_HEADERS=

# Application Settings
APP_TIMEZONE=Asia/Jakarta

# Heartbeat Codes (comma-separated, optional)
# Events with these codes won't be forwarded
HEARTBEAT_CODES=YK,RP
